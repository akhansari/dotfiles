[main]

# Enables auto expand mode, which is similar to `\x auto` in psql.
# auto_expand = True

pager = 'ov -C -d "│" --section-delimiter "^┌" --section-header-num 3 --column-mode --wrap=false'
table_format = psql_unicode

vi = True
