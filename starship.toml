
[git_status]
up_to_date = " "
stashed = " "
ahead = "⇡${count} "
diverged = "⇕⇡${ahead_count}⇣${behind_count} "
behind = "⇣${count} "
staged = " "
untracked = "󱙝 "
format = "([$all_status$ahead_behind]($style) )"

[aws]
style = 'bold blue'
symbol = '🅰 '

[aws.region_aliases]
eu-west-1 = 'eu'
